upstream flask_app {
    server web:8000;
}

# HTTP server
server {
    listen 80;
    listen [::]:80;
    server_name _;

    location / {
        proxy_pass http://flask_app;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    location /health {
        proxy_pass http://flask_app/health;
        access_log off;
    }
}

# HTTPS server with self-signed certificate
# Uncomment the block below after SSL certificates are generated
# To enable HTTPS:
# 1. Generate SSL cert: ./setup-self-signed-ssl.sh YOUR_SERVER_IP
# 2. Uncomment lines below (remove the # at the start of each line)
# 3. Reload nginx: docker compose restart nginx

# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name _;
#
#     ssl_certificate /etc/nginx/ssl/fullchain.pem;
#     ssl_certificate_key /etc/nginx/ssl/privkey.pem;
#
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_prefer_server_ciphers on;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     location / {
#         proxy_pass http://flask_app;
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#     }
#
#     location /health {
#         proxy_pass http://flask_app/health;
#         access_log off;
#     }
# }